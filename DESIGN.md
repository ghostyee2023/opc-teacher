---
name: 共场学生端
description: 面向课堂协作的移动工作台
colors:
  primary: "#087f73"
  primary-deep: "#075f57"
  canvas: "#eef2f1"
  surface: "#ffffff"
  surface-2: "#f6f9f8"
  line: "#d7e2de"
  warning: "#c16f25"
typography:
  display:
    fontFamily: "SF Pro Display, PingFang SC, Microsoft YaHei, system-ui, sans-serif"
    fontSize: "clamp(1.8rem, 4vw, 2.4rem)"
    fontWeight: 700
    lineHeight: 1.2
    letterSpacing: "-0.04em"
  title:
    fontFamily: "SF Pro Display, PingFang SC, Microsoft YaHei, system-ui, sans-serif"
    fontSize: "clamp(22px, 3vw, 30px)"
    fontWeight: 650
    lineHeight: 1.2
    letterSpacing: "-0.035em"
  body:
    fontFamily: "SF Pro Display, PingFang SC, Microsoft YaHei, system-ui, sans-serif"
    fontSize: "16px"
    fontWeight: 400
    lineHeight: 1.7
  label:
    fontFamily: "SF Pro Display, PingFang SC, Microsoft YaHei, system-ui, sans-serif"
    fontSize: "12px"
    fontWeight: 700
    lineHeight: 1.4
rounded:
  sm: "8px"
  md: "12px"
  lg: "16px"
spacing:
  xs: "4px"
  sm: "8px"
  md: "12px"
  lg: "16px"
  xl: "24px"
  2xl: "32px"
components:
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "#ffffff"
    rounded: "{rounded.sm}"
    height: "44px"
    padding: "0 20px"
  button-secondary:
    backgroundColor: "{colors.surface-2}"
    textColor: "{colors.primary-deep}"
    rounded: "{rounded.sm}"
    height: "44px"
    padding: "0 16px"
  card:
    backgroundColor: "{colors.surface}"
    textColor: "#172624"
    rounded: "{rounded.lg}"
    padding: "20px"
---

# Design System: 共场学生端

## Overview

**Creative North Star: “课堂剪辑台”**

学生端把一节课当成一条可定位的工作轨道：章节是轨道，任务和互动是正在处理的片段，学生的动作会留下清晰的状态标记。视觉上以冷灰纸面和白色工作面承载内容，以青绿色表达可执行动作，以少量橙色标记当前片段，既保留共场品牌识别，也让移动端有独立记忆点。

内容保持单列和高可读性，固定的顶部章节入口与底部操作轨道让学生始终知道“我在哪一章、下一步做什么”。动效只在完成、提交、切换等真实动作发生时出现。

**Key Characteristics:**

- 单列工作面，任务内容优先
- 上下两条稳定操作轨道
- 青绿色动作色，橙色定位标记
- 44px 触控热区与安全区适配

## Colors

冷灰纸面、白色工作面与青绿色动作色构成基础，橙色只用于当前任务定位。

### Primary

- **共场青绿** (#087f73): 主要按钮、选中状态和完成反馈。
- **深青绿** (#075f57): 文字状态和高对比强调。

### Secondary

- **胶带橙** (#c16f25): 任务轨道当前标记和少量状态提示，不承担主要操作。

### Neutral

- **冷灰纸面** (#eef2f1): 页面背景。
- **工作白** (#ffffff): 卡片和输入区域。
- **雾面灰** (#f6f9f8): 次级容器和未选状态。
- **轨道线** (#d7e2de): 分隔线和边界。

**The One-Accent Rule.** 青绿色是唯一主操作色；橙色只作为定位标记出现，不用于第二套 CTA。

## Typography

**Display Font:** SF Pro Display, PingFang SC, Microsoft YaHei, system-ui, sans-serif
**Body Font:** SF Pro Display, PingFang SC, Microsoft YaHei, system-ui, sans-serif

**Character:** 中文阅读优先，标题有明确重量，正文保持 16px 和 1.7 倍行高，辅助信息不低于 12px。

### Hierarchy

- **Display** (700, clamp 28px–42px, 1.18): 任务卡标题和互动题目。
- **Page title** (650, clamp 22px–30px, 1.2): 页面级标题，例如任务、小组和“我的”。
- **Title** (760, 20px, 1.3): 卡片标题和页面重点。
- **Body** (400, 16px, 1.7): 任务内容、提示词和问题。
- **Label** (700, 13px–14px, 1.4): 卡片标题、区块标签和按钮。
- **Caption** (400–700, 12px, 1.4–1.5): 导航、状态、排名辅助说明。

学生端实现以 `12 / 14 / 16 / 20px` 为基础阶梯，并在样式文件末尾设置最终覆盖层，防止局部组件重新出现 11px 或 13px 小字。移动端页面标题为 24px，任务标题为 28–34px；桌面端任务标题最高 42px。

## Layout

移动端采用单列内容，最大宽度 720px，手机视口使用 12px 侧边距，内容底部预留固定操作栏空间。间距遵循 4 / 8 / 12 / 16 / 24 / 32px 节奏。顶部章节菜单和底部导航固定在安全区域内，主内容始终可滚动且不被遮挡。

## Elevation & Depth

采用轻量环境阴影和色调分层，不使用黑色硬投影。卡片用白色工作面与 1px 轨道线建立层级，提问输入和菜单出现时才使用更明显的浮层阴影。

## Shapes

卡片使用 16px 圆角，普通控件使用 8px，分段切换使用 8px。提问按钮保留圆形作为唯一高辨识度例外，其余控件不使用过度圆润的胶囊形。

## Components

### Buttons

- **Shape:** 8px 圆角，最小高度 44px。
- **Primary:** 青绿色底、白色文字，用于提交、保存和完成。
- **Hover / Focus:** 轻微提亮与 3px 青绿色 focus ring。
- **Disabled:** 保留布局尺寸，降低透明度并显示不可用光标。

### Cards / Containers

- **Corner Style:** 16px。
- **Background:** 工作白或雾面灰。
- **Shadow Strategy:** 只用环境阴影，避免每一层都悬浮。
- **Border:** 1px 轨道线。
- **Internal Padding:** 20px，手机视口收敛到 16px。

### Inputs / Fields

- **Style:** 雾面灰背景、1px 轨道线、8px 圆角、44px 高度。
- **Focus:** 青绿色边框和可见 focus ring。

### Navigation

顶部为章节菜单与课堂状态，底部为任务 / 互动 / 提问 / 小组 / 我的五项导航。底部提问按钮作为中央凸起动作，其他导航项保持同等热区和对齐。

教师端的“学生端预览”采用独立页面入口，点击后新窗口打开真实 `/student` 页面，不在教师侧重复维护一套手机模拟器状态。

课堂配置使用不透明工作面弹窗，内部以同级标签切换“课堂信息 / 教师信息 / 分组设置”；课堂信息中优先展示班级名称和课程主题，避免把班级概念隐藏在教师资料里。

课堂状态菜单同时承担课程生命周期管理：开设新课作为根节点动作，结束课程需要确认，结束后仍可导出当前课程的 Excel 数据快照。

课程管理菜单提供课程切换和 JSON 课程包导入。课程包以“课程 → 章节 → 任务 / 互动”的层级组织内容，切换课程时目录、任务和互动题目同步切换，避免只更换标题而保留旧内容。

新增课程与编辑课程使用不同配置模式：新增模式以创建为终点，编辑模式以更新当前课程为终点；两者通过标题、说明和按钮文案明确区分。

原型覆盖审计（已被功能版覆盖）：教师讲义工作区、课堂大盘、提问处理、独立学生端、课堂生命周期入口、学生扫码/课堂码入场与加入小组、课程内容编辑（目录 / 任务 / 提示词 / 互动题）、分组管理和互动题编辑已全部落地为真实数据流；实时同步、课堂结束报告与积分明细也已实现，素材管理仍列为后续项。

学生端入场页为“课堂码 → 查找课堂 → 昵称 → 小组”的顺序表单；教师端在“创建课堂”落地页完成课程选择与课堂码生成，课堂配置弹窗聚焦教师信息 / 分组 / 积分规则，课程内容编辑统一收敛到 `/manage` 管理页。数据层为事件溯源：课堂创建时对课程内容做快照，进行中的课堂不随管理页编辑漂移。

## Do's and Don'ts

### Do:

- **Do** 用青绿色表达可以执行的动作。
- **Do** 保持正文 16px、辅助信息 12px 以上。
- **Do** 为固定栏预留安全区和滚动空间。
- **Do** 让动效解释状态变化，而不是装饰性循环。

### Don't:

- **Don't** 在学生端混用第二套组件库。
- **Don't** 用颜色单独表达完成、错误或排名状态。
- **Don't** 把章节、任务和任务卡重复显示成多个标题。
- **Don't** 用小于 44px 的主要触控目标。

## P1 设计决策与进度

### 课堂连接与同步

- 连接状态使用三块同宽状态卡（网络、章节、在线人数），保证教师投屏时一眼读取。
- 跟随 / 自由浏览采用二选一大触控卡；学生锁定作为单独开关，避免与导航状态混淆。

### 提问回答闭环

- 问题聚焦页的回答区采用“可见范围 → 发送回答 / 发送并解决”的动作顺序。
- 公开回答与仅小组可见使用同一组分段按钮，状态写入原型事件通道，学生端以轻提示承接，不遮挡任务内容。

### 积分与报告

- 加分不只更新总分，同时记录理由、分值和时间，详情抽屉内可追溯。
- 课堂结束报告使用指标摘要、排行条和荣誉卡三层结构；导出 Excel 保留为明确的次级动作。
- 积分规则放在课程配置的独立标签中，以“行为说明 + 数字输入 + 分值预览”组织，避免把规则散落在排行榜和加分抽屉里。

## 原型打磨审查（2026-09-14）

- 所有配置类弹窗必须使用不透明工作面、独立遮罩和内容隔离；课件标题不应穿透弹窗。
- 弹窗标题、说明、表单和主按钮保持单一纵向阅读流，避免底层大字号元素与弹窗内容形成视觉重叠。
- 后续验收重点：键盘焦点可见、窄屏不横向溢出、课程切换后学生端内容与积分规则一致、无数据时仍有明确下一步动作。

## 独立课程管理页

- `/manage` 是课前编辑场景，使用左侧课程列表、章节目录和右侧内容编辑器的三栏层级；课堂 `/` 保持实时授课工作区单一职责。
- 提示词采用章节内列表，可按步骤拆分成多个提示词；默认提示词保留“默认”标记，新增提示词不改变已有任务结构。
- 课程管理入口放入右上角课程菜单，与课程切换、导入等操作保持同一入口层级。
- 入口收口调整：左下角仅保留教师信息，管理页入口放入右上角课程菜单，避免课堂侧边栏出现重复的配置入口。
- 管理页标签采用正常文档流 + `position: sticky`，顶部标签高度计入主内容布局；设置子页使用同一内容起点，不允许覆盖课程标题。
- 管理页头部使用两层节奏：72px 页面身份层 + 57px 管理导航层；两层连续排列并固定在顶部，下面内容只在工作区滚动。
- “互动题目”与“课程内容”同级，章节选择器与题目编辑器保持同一上下文，避免教师误以为互动题只属于整门课程。

## 管理页布局续作（2026-09-14）

- 课程内容页改为“左侧课程目录 + 右侧内容编辑”单一工作面；章节切换与编辑内容在同一层级，不再由覆盖层遮住目录。
- 右侧编辑区使用三个并列子标签：实操任务、提示词、互动题目；顶层重复的互动入口已移除。
- 保留每章多个提示词与独立互动题配置，保存按钮固定在当前章节编辑区标题行，减少上下文跳转。
- 中等视口下目录与编辑器按比例收缩，窄屏时目录转为横向章节条，避免固定宽度造成横向溢出。

## 功能版设计决策（2026-09-14 · 事件溯源重构）

### 教师落地页（创建课堂）

- 首次进入 `/` 不再直接进入工作区，而是“选课程 → 课堂码 → 二维码 → 开始课堂”的落地页，把“开课”变成一个明确动作。
- 课堂码用 ui-monospace 24px + 0.22em 字距的虚线框呈现，点击 chip / 链接按钮复制学生加入链接；二维码 96px 带白底描边，深色模式下白底保持不变以保证扫码率。
- 课堂连接中 / 错误态使用与落地页同语言的居中 boot-state（图标 + 标题 + 模式说明），不复用学生端加载卡片。

### 学生入场页

- 课堂码输入与“查找课堂”按钮组成同行操作，查找成功后才展开小组选择，避免学生对着无效课堂码选小组。
- 未查找到课堂时给出来自后端的真实反馈（“课堂码不存在 / 该课堂已结束”），本地模式额外提示“需与老师同一浏览器”。
- 学生端页面标题（`document.title`）随路由区分：学生课堂 / 内容管理 / 教师控制台。

### 主题与 token

- 设计 token 从 `.app-shell` 提升到 `:root` 与 `:root[data-theme="dark"]`，三个端的页面容器仍保留局部覆盖；主题切换同时写 `<html data-theme>`，Radix portal 弹窗（Dialog/Dropdown）在 body 下也能继承正确的明暗 token。
- 此前 P1 区块引用的未定义 token（`--ink` / `--primary` / `--primary-deep`）已统一映射为 `--text` / `--accent` / `--accent-deep`，移除永久走 fallback 的隐患。
- 金色系（#bb8a36 / #c99329 / #a87824 等）全部收敛到 `--warning` / `--warning-soft`，One-Accent Rule 重新成立：奖牌、领奖台、排行榜徽章、烟花点缀都来自 warning 系。

### 响应式与可访问性

- 教师端解除 `min-width: 1120px`：≤1120px 折叠右栏（课堂提问可通过顶栏菜单的同步弹窗触达）、≤880px 进一步折叠左栏为图标栏，工作区改单列。
- 手机预览摘要（preview-live-summary）替代原 phone-frame 死 DOM：用三行“章节 / 在堂人数 / 领先组”真实数据替代假手机模型，学生端预览统一走“打开学生端”链接（自动带课堂码）。
- 补全 `.app-shell` / `.management-page` / `.student-page` 内 button / input / textarea 的 `:focus-visible` 焦点环（2px accent + 2px offset）。
- 手机预览区 9–11px 违规小字号全部提升到 12px；学生分组触控目标 42px → 44px。

### 状态与数据反馈

- 所有成功 toast 基于真实操作结果（如剪贴板复制成功/失败分开设文案，复制失败提示手动复制），不再出现与事实不符的“已复制”。
- 教师端右栏“学生端预览”标签的副标题改为“实时数据”，摘要行随事件流实时更新，避免教师误以为在预览快照。

## 第二轮遗漏复检设计决策（2026-09-14）

### 教师端

- 讲义页码提升为课堂会话字段（slideIndex）：翻页/页码点/切章统一走 patchSession，刷新保持、可与学生端同步， 当前页会同步给学生的文案重新成立。
- 「前往支援」从纯 UI toast 改为 support 事件：学生端按小组维度收到实时提醒（20 秒窗口内弹 toast，避免历史事件重放骚扰）。
- 写入失败横幅（sync-error-banner）：危险色系、居中悬浮于顶栏下方，提供重试 / 知道了双出口；作为乐观 UI 与真实写入结果的对账出口，替代永远显示成功的假象。
- 教师信息改为草稿模式：姓名/角色/寄语编辑只更新本地草稿，点「保存教师信息」一次 patchSession，消除每键一次的全量写库。
- 倒计时归零后「继续」按本轮任务时长重启，不再原地续零；顶栏菜单与左下角教师资料双入口统一打开配置弹窗并定位教师信息标签。

### 学生端

- 互动页与任务页共用同一「老师还没有发布本节任务」等待卡，发布前答题入口完全一致地锁定。
- 「老师已回复」横幅改为按 question id dismiss：同一条回复只提示一次，老师对后续新问题的回复会重新弹出，不再永久静音。
- 网络错误与课堂不存在分离（store.notFound）：断网/服务异常时保留本地会话并提示重试，只有确认课堂码不存在才清理，避免 studentId 失联导致的统计虚高。
- 「退出课堂」发送 leave 事件：折叠时从成员统计移除，在堂人数实时联动。

### 数据层与管理页

- 事件联合新增 support / leave 两个成员，折叠器保持幂等（leave 直接移除学生，重复 leave 无副作用）。
- patchSession 写库串行队列：本地 sessionRef 同步更新 + 服务端按序写，防 Supabase 并发乱序旧覆盖新；refresh 增加迟到响应保护（课堂码切换后丢弃过期结果）。
- 管理页课程包导入做结构校验（标题字符串、章节非空对象、每章 title、互动题 answers 非空字符串数组且 correct 在界内），畸形文件给 toast 反馈而非白屏。
- 空章节提示词统一归一为至少一个空位，管理页编辑器与学生端展示一致。
