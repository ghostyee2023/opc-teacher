# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

学生在课堂中使用手机端参与教师发布的实操任务、互动题和提问；教师使用课堂控制台组织章节、分组、积分与课堂大盘。

## Product Purpose

共场 AI 课堂互动系统把教师讲义、课堂任务和学生反馈连接在同一场课堂里，让学生能快速完成任务、回答互动题并向老师提问，让教师能实时看到课堂进展。

## Positioning

产品的核心机制是“讲义驱动的课堂协作”：教师发布当前章节任务，学生在独立移动端完成个人操作，互动结果与小组积分同步回课堂大盘。

## Operating Context

教师端需要在课堂投屏上展示大盘与 PPT HTML；学生端主要在移动浏览器中使用，底部固定导航承载高频操作。

## Capabilities and Constraints

- 学生端独立入口为 `/student`，支持 `?code=` 预填课堂码；包含任务、互动、提问、小组、我的五个入口。
- 学生入场流程：输入课堂码 → 查找课堂（校验真实存在）→ 昵称 → 选择小组 → 进入课堂；入场、切组、切换组长都会作为 join 事件同步到教师端。
- 任务页包含任务卡和按步骤组织的锦囊提示（每章多个提示词，可复制），学生点击“我完成了”会向教师端提交 complete 事件并按规则给小组加分。
- 互动题按章节独立配置（未配置时回退课程默认题）；学生答题后本题锁定，答对按规则给小组加分。
- 学生可发起提问（输入框支持点击页面其他区域收起）；相同问题自动聚合同问人数，教师回答可公开全班或仅小组可见，学生端收到“老师已回复”横幅。
- 学生可切换所在小组并标记组长身份；组长可以填写本组最关心的三个问题，教师端小组面板可见。
- 教师端为“创建课堂 → 课堂工作区”两段式：落地页选课程、生成 6 位课堂码与学生扫码链接；工作区含章节导航、讲义放映、课堂大盘、提问处理、课堂配置。
- 教师可切章/发布任务（带统一倒计时，三端一致）/切换跟随模式（强/软/自由）/锁定学生章节/确认小组完成/加分/结束课堂/导出 Excel 报告。
- 管理页 `/manage` 负责课前内容：课程库（多课程切换、JSON 课程包导入预览）、章节内容、每章多提示词、互动题、分组模板与积分规则默认值。
- 数据同步：配置 Supabase 环境变量时跨设备实时同步；未配置时本地 localStorage 模式（同浏览器多标签）。课堂创建时对课程内容做快照，进行中的课堂不受管理页后续编辑影响。
- 已知边界：无登录体系，Supabase 行级安全为原型开放策略；本地模式学生须与教师同一浏览器；学生撤销完成时已得积分保留。

## Brand Commitments

产品名称为“共场”，现有原型使用青绿色为主色、浅色与深色主题，以及 Radix Themes 和 Phosphor Icons。

## Evidence on Hand

- 现有实现：`src/types.ts`（共享类型）、`src/lib/backend.ts`（双后端适配）、`src/lib/classroom.ts`（事件折叠）、`src/hooks/useClassroom.ts`（课堂 store）、`src/teacher/TeacherApp.tsx`、`src/student/StudentApp.tsx`、`src/manage/ManageApp.tsx`、`src/styles.css`。
- 部署配置：`supabase/schema.sql`、`vercel.json`、`.env.example`。
- 产品设计记录：`AI课堂互动系统-产品设计文档.md`。
- 当前学生端页面：`http://127.0.0.1:5173/student`。

## Product Principles

- 学生在几秒内理解当前要做什么。
- 高频动作始终在拇指可达区域。
- 个人完成状态与小组进度清晰区分。
- 积分和动效用于鼓励参与，不遮挡任务内容。
- 教师投屏与学生手机是不同场景，分别优化信息密度。

## Accessibility & Inclusion

学生端需要支持键盘 focus-visible、清晰的状态文本、至少 44px 的触控目标、浅色/深色主题和减少动态效果偏好。

## 进度记录

### P1 首批（2026-09-14）

- 课堂连接与同步状态：网络、在线人数、章节指针、跟随 / 自由浏览、学生锁定和立即同步。
- 教师回答闭环：回答草稿、全班 / 小组可见范围、发送回答、标记解决；学生端通过本地事件提示教师回复。
- 积分与课堂报告：小组加分事件明细、奖励理由、结束报告指标、积分排行和 Excel 导出前预览。
- 积分规则设置：教师可配置任务完成、互动答对、提问和教师奖励分值，学生端按同一规则显示积分反馈。

### 原型打磨审查（2026-09-14）

- 配置、积分和导入弹窗已强化遮罩与不透明内容层，避免底层课件文本穿透。
- 下一轮重点是跨课程数据一致性、窄屏 / 键盘验收和空状态引导；这些属于从可演示原型走向可试用原型的最后一段打磨。

### 独立课程管理页（2026-09-14）

- 新增 `/manage` 课程管理入口，支持课程目录、章节内容和多个提示词维护。
- 课堂工作区聚焦“当前章节播放与发布”，内容管理页聚焦“课前编辑与保存”，职责分离。
- 教师资料保留在左下角快捷入口，课程、内容、分组和积分设置统一从右上角菜单进入管理页。
- 管理页顶部分类导航改为吸顶但不覆盖内容的布局，确保课程标题和编辑区保持稳定对齐。
- 每个章节的互动题目在管理页独立维护，不再只有课程级单题。

### 管理页布局续作（2026-09-14）

- 课程目录与章节编辑器改为同一横向工作区，右侧直接切换实操任务、提示词、互动题目。
- 删除重复的顶层互动题目导航，避免配置入口分散；移动端目录改为可横向滚动的章节条。
- 响应式布局在平板和手机视口下收敛为单列，确保章节和编辑字段不被裁切。

### 从原型到功能版:事件溯源 + 双后端（2026-09-14）

- 整体重构:从"单文件 + 各端独立假状态"改为"课堂会话 + 追加式事件流 + 各端幂等折叠",三端共享同一份事件源,课堂流程闭环全部打通并经浏览器回归验证(入场、发布、完成、互动、提问回答、确认加分、结束导出)。
- 后端双模式:配置 `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` 时走 Supabase 实时同步(跨设备,Realtime 订阅 classrooms + classroom_events);未配置时自动降级 localStorage 本地模式(同浏览器多标签)。表结构见 `supabase/schema.sql`,Vercel 部署用 `vercel.json` rewrites 解决刷新 404。
- 教师端新增"创建课堂"落地页:选课程 → 生成 6 位课堂码 → 学生扫码或输码加入;课堂创建时从课程库快照内容,管理页编辑只影响后续新课堂。
- 教师切章/发布/跟随模式/学生锁定、学生加入/切换小组组长/完成任务/互动答题/提问/提交小组问题,教师回答(全班或小组)/确认完成/加分,全部为真实跨端事件;大盘、报告、导出均为真实数据。
- 学生端支持 `?code=` 预填;倒计时改为共享时间戳,三端一致;教师顶栏新增课堂码 chip 复制加入链接。
- 修复原型审查的结构问题:移除 courseStatus 镜像、删除管理页重复互动面板与死代码 tab、统一 prompt/prompts 双存、倒计时 effect 依赖、排行榜 useMemo、入场与事件写入竞态(本地后端写队列串行化)。
- 样式:未定义 token(--ink/--primary)映射到设计系统 token,主题提升到 `<html data-theme>` 供 portal 弹窗继承;金色越界色收敛 warning 系;违规小字号提至 12px;教师端解除 1120px 最小宽度并新增 1120/880px 折叠断点;补键盘 focus-visible;触控目标 44px。
- 已知边界:本地模式学生须与教师同浏览器;无登录体系,Supabase RLS 为原型开放策略;学生撤销完成时已得积分保留。

### 功能版回归验收（2026-09-14）

- 浏览器全流程回归 12/12 通过：创建课堂 → 发布任务 → 学生入场（一次成功）→ 完成任务 → 互动答题 → 提问 → 学生刷新后会话与状态保留 → 教师大盘显示真实在堂学员与小组进度 → 回答并解决 → 确认完成加分 → 学生收到回复横幅 → 课堂码 chip 复制。
- 首轮测试发现并修复 3 个高严重性缺陷：两端"首次点击必失败"的 resume-check 竞态（loading 改为渲染期派生）、LocalBackend 事件写入读改写竞态（引入串行写队列，5 类事件不再丢失）、学生入场未发送 join 事件（改为幂等 join effect，恢复会话时自动补发）。
- 剪贴板操作增加 execCommand 降级路径，失败时提示"复制失败,请手动复制"而非虚假成功。
- 积分链路完整可追溯：任务 +10 → 答对 +5 → 提问 +1 → 确认 +5，大盘、小组面板与导出一致。

### 全量遗漏复检与第二轮修复（2026-09-14）

- 全量复检三端共 13 处遗漏并全部修复，二轮回归 8/8 通过、console 零错误：
- **教师端**：「前往支援」从假动作改为 support 事件（学生端实时 toast 提醒）；讲义页码（slideIndex）从本地 state 提升为会话字段，翻页/切章同步学生且刷新保持；新增写入失败横幅（error + 重试入口），乐观 toast 不再谎报成功；「立即同步」改为完成后提示；顶栏菜单与左下角教师资料入口统一停到教师信息标签；倒计时归零后继续按本轮时长重启；教师信息改为草稿模式、点保存一次写库（避免每键一次网络写）。
- **学生端**：互动题增加发布前锁定（未发布显示等待卡，任务/互动两处一致）；「老师已回复」横幅改为按问题 id dismiss，老师的新回复会重新弹出；网络错误不再误判为"课堂不存在"（store 新增 notFound 区分，保留本地会话）；「退出课堂」发送 leave 事件，在堂人数实时联动；切组/切组长补课堂结束拦截。
- **数据层**：新增 support / leave 事件类型并纳入折叠（leave 从成员统计移除）；patchSession 写库串行化（防 Supabase 并发乱序旧覆盖新）；refresh 增加迟到响应保护；管理页 JSON 课程包导入结构校验（标题/章节/互动题逐项校验，畸形文件不再白屏）；空章提示词归一为至少一个空位，编辑器与学生端一致；删除未使用 import。
