---
title: AI课堂互动系统产品设计文档
type: product-design
status: prototype
version: 0.1
updated: 2026-09-14
product_type: Web App / PWA
target_scale: 单课堂最多100名学员，8-12个小组，1位老师
deployment_candidate: Vercel + Supabase
---

# AI课堂互动系统产品设计文档

## 1. 产品概述

### 1.1 产品定位

面向线下企业 AI 培训的实时互动课堂系统。

系统由三部分组成：

1. 老师端：控制课堂节奏、发布任务、查看小组状态、处理提问。
2. 学生端：通过手机扫码进入课堂，查看课程内容、完成任务、提问和参与小组 PK。
3. 课堂大屏端：展示当前章节、课堂进度、小组状态和排行榜，营造 AI 课堂氛围。

产品不是传统 LMS，也不是视频会议系统。它的核心是：

> 老师掌控节奏，学生同步行动，小组共同完成，课堂状态实时可见。

### 1.2 目标规模

- 单个课堂最多 100 名学员。
- 预计 8-12 个小组。
- 默认 1 位老师控制课堂。
- 学员通过手机浏览器使用，无需安装 App。
- 课程内容由老师制作成 HTML 页面后导入。

### 1.3 第一成功结果

一位老师可以在一堂真实线下课中完成以下闭环：

```text
创建课堂
→ 学员扫码进入
→ 学员创建或加入小组
→ 老师发布章节和任务
→ 学员查看讲义、复制贴词、下载素材
→ 学员点击完成或提交问题
→ 老师查看8-12个小组的完成情况
→ 小组获得积分并更新排行榜
→ 课堂结束后导出结果
```

## 2. 角色与权限

### 2.1 老师

- 创建、开始、暂停和结束课堂。
- 导入课程目录、课程 HTML 和互动配置。
- 发布当前章节、任务和课堂提示。
- 选择课程跟随模式。
- 查看所有小组的完成状态。
- 查看、回答、置顶和关闭学员问题。
- 手动加分、扣分和发放奖励。
- 转移小组组长权限。
- 查看并导出课堂复盘报告。

### 2.2 组长

- 代表小组提交任务。
- 确认小组名称。
- 发起组内投票。
- 查看小组成员和小组进度。

组长不拥有修改最终积分、修改课程和修改老师状态的权限。

### 2.3 普通学员

- 扫码进入课堂。
- 输入昵称。
- 创建或加入小组。
- 提出小组名称。
- 查看已开放的课程内容。
- 复制贴词、下载素材。
- 完成课堂任务。
- 提交问题。
- 查看小组进度和排行榜。

第一版不强制要求学生注册账号或绑定手机号，使用课堂临时身份即可。

## 3. 课程内容模型

### 3.1 HTML课程包

每个课程以一个版本化的 HTML 课程包导入：

```text
course-package/
├── manifest.json
├── chapters/
│   ├── 01.html
│   ├── 02.html
│   └── 03.html
├── assets/
│   ├── images/
│   ├── files/
│   └── icons/
└── interactions.json
```

文件职责：

- `manifest.json`：课程标题、版本、章节目录、排序和封面。
- `chapters/*.html`：课程正文、案例、图片、讲义内容。
- `assets/`：图片、模板、PDF、表格等素材。
- `interactions.json`：任务、完成按钮、提问点和积分规则。

### 3.2 课程版本

每次课堂必须绑定一个固定的课程版本：

```text
课程：AI企业培训
版本：v1.3
课堂：2026-09-11上午班
```

课程内容更新时创建新版本，不能直接覆盖正在使用的版本。这样可以保证同一堂课的所有学员看到相同内容，也便于课后复盘和回滚。

### 3.3 内容与互动分离

课程 HTML 负责“讲什么”，互动配置负责“课堂怎么做”。

建议使用声明式互动标记：

```html
<button data-action="copy" data-text="请先确认目标、受众和限制条件">
  复制贴词
</button>

<div data-task-id="task-02" data-action="complete">
  完成任务后点击这里
</div>
```

正式课程优先使用安全课程模式：过滤脚本、危险内联事件和未知外部资源；互动通过白名单动作提供。可信课程包可以使用沙箱 iframe，通过 `postMessage` 与外框通信。

## 4. 产品外框与页面结构

### 4.1 桌面端结构

```text
┌────────────────────────────────────┐
│ 课程名 / 当前章节 / 网络状态 / 课堂状态 │
├──────────────┬─────────────────────┤
│ 课程目录       │                     │
│ 章节1          │     HTML内容区域     │
│ 章节2          │                     │
│ 章节3          │     讲义、贴词、案例   │
│                │     素材下载           │
├──────────────┴─────────────────────┤
│ 任务 / 完成 / 提问 / 小组 / 积分 / 素材 │
└────────────────────────────────────┘
```

### 4.2 手机端结构

- 顶部显示课程名、当前章节和网络状态。
- 课程目录改为抽屉或弹层。
- 中间区域显示 HTML 讲义内容。
- 底部固定显示互动入口：任务、提问、小组、素材。
- 当前任务和“已完成”按钮优先于其他功能。

### 4.3 课堂大屏

大屏只展示小组和课堂信息，不展示个人隐私：

- 当前章节。
- 当前任务。
- 倒计时。
- 8-12个小组的完成进度。
- 当前领先小组。
- 最佳协作组或最佳提问组。
- 老师发布的提示。

## 5. 课程跟随与锁定模式

系统提供三种模式。

### 5.1 强跟随模式

适合老师统一讲解和演示。

- 学员只能查看老师当前页面或节点。
- 不允许跳到未开放章节。
- 可以回看已经完成的内容。
- 老师切换页面时，学员自动跟随。

### 5.2 软跟随模式（默认）

适合课堂练习。

- 学员默认跟随老师。
- 可以回看已经开放的内容。
- 不能提前查看未开放章节。
- 老师切换章节时，学员收到提示并可回到当前内容。

### 5.3 自由浏览模式

适合复习、答疑和课后使用。

- 学员可以自由浏览已发布课程内容。
- 页面持续显示“老师当前正在讲……”的提示。
- 任务和积分仍然按照课堂状态运行。

课堂状态中的核心字段：

```json
{
  "current_chapter": "chapter-02",
  "current_block": "task-03",
  "follow_mode": "soft",
  "unlocked_chapters": ["chapter-01", "chapter-02"],
  "revision": 28
}
```

老师端拥有课堂指针的最终控制权，学生端不能直接修改。

## 6. 小组机制与PK

### 6.1 入组流程

```text
扫码或输入课堂码
→ 输入昵称
→ 创建小组或加入小组
→ 查看组内成员
→ 进入课堂
```

二维码下方同时显示 6 位备用课堂码，二维码失效时可以手动输入。

### 6.2 组长机制

- 创建者先成为临时组长。
- 小组成员可以发起组长投票。
- 老师可以随时转移组长权限。
- 没有组长时，小组仍然可以完成任务。

### 6.3 小组命名权

- 每个成员都可以提出一个小组名称。
- 小组成员投票选择候选名称。
- 组长或老师最终确认。
- 名称必须唯一，并经过敏感词过滤。

### 6.4 积分维度

不只按照速度排名，建议采用：

| 维度 | 建议权重 |
|---|---:|
| 任务完成 | 40% |
| 任务质量 | 30% |
| 小组协作 | 20% |
| 挑战任务 | 10% |

可以展示多种荣誉：

- 当前领先组。
- 最佳协作组。
- 最佳提问组。
- 进步最快组。
- 最需要支援组。

积分必须由服务器计算，客户端只能提交事件，不能直接修改分数。

## 7. 章节任务与完成状态

每个任务至少支持以下状态：

- 未开始。
- 进行中。
- 学员自报完成。
- 教师确认完成。
- 卡住，需要帮助。

老师端的小组状态矩阵示例：

| 小组 | 任务状态 | 进度 | 最近动作 |
|---|---|---:|---|
| A组 | 教师已确认 | 100% | 已提交案例 |
| B组 | 学员自报完成 | 80% | 等待确认 |
| C组 | 进行中 | 40% | 2人未完成 |
| D组 | 需要帮助 | 20% | 提交3个问题 |

“学员点击完成”不等于“任务质量已经确认”，老师可以单独确认或退回任务。

## 8. 学员提问

提问入口固定放在互动区：

```text
我遇到的问题是……

问题类型：
□ 不理解概念
□ 不知道怎么操作
□ 素材找不到
□ 小组意见不一致
□ 其他
```

问题状态：

- 待回答。
- 老师已看到。
- 老师回答。
- 转为公共问题。
- 转为小组任务。
- 已解决。

后续可加入 AI 辅助聚合同类问题和生成回答草稿，但 AI 不应成为课堂核心链路的强依赖。

## 9. 数据同步架构

### 9.1 推荐部署

```text
学生手机 / 老师电脑
        ↓
Vercel：Next.js前端、PWA和课程外框
        ↓ HTTPS API
Supabase：PostgreSQL、Auth、Realtime、Storage
        ↓
IndexedDB：课程缓存和断线操作队列
```

Vercel 负责网页部署和前端访问，不单独承担主数据库或长连接实时服务。

### 9.2 同步数据分层

课堂级状态：

- 当前章节。
- 当前页面或任务。
- 跟随模式。
- 倒计时。
- 老师提示。

小组级状态：

- 小组人数。
- 完成数量。
- 卡住人数。
- 小组积分。
- 最近活动时间。

学生个人状态：

- 加入小组。
- 点击完成。
- 提交问题。
- 下载或复制记录。

实时频道只广播课堂状态和 8-12 个小组的摘要；学生个人操作通过 API 写入服务器，不直接广播给全部学员。

### 9.3 事件队列

每个学生操作都带有唯一事件 ID：

```json
{
  "event_id": "evt-8f2a",
  "type": "TASK_COMPLETED",
  "student_id": "student-01",
  "group_id": "group-a",
  "task_id": "task-02",
  "client_time": "2026-09-11T10:20:00+08:00"
}
```

服务器处理逻辑：

1. 检查事件 ID 是否已处理。
2. 已处理则直接返回成功，避免重复积分。
3. 未处理则写入事件表。
4. 更新任务、小组和积分状态。
5. 广播最新小组摘要。

### 9.4 冲突处理

- 老师课堂指针：以服务器上的老师最后一次有效指令为准。
- 学员完成记录：按学员和任务幂等处理。
- 小组积分：使用追加式积分事件，不直接覆盖总分。
- 课程内容：课堂绑定固定版本，不在课堂中途覆盖。
- 网络恢复：先上传本地未同步事件，再拉取服务器最新快照。

## 10. 弱网与断网设计

### 10.1 正常在线

- 老师发布状态后实时同步。
- 小组进度和排行榜即时更新。
- 课程内容从缓存或 CDN 加载。

### 10.2 弱网

- HTML、文字和当前章节提前缓存。
- 学员操作先写入 IndexedDB 队列。
- 网络恢复后自动重试。
- 页面显示“已同步”或“等待同步”。
- 核心操作不依赖图片或视频加载。

### 10.3 完全断网

后续增加本地课堂模式：

```text
老师电脑或小型主机
        ↓
现场Wi-Fi或稳定热点
        ↓
学员手机访问本地课堂地址
```

本地课堂模式使用同一套事件协议，网络恢复后再同步至云端。

## 11. 100人课堂的性能目标

| 场景 | 目标 |
|---|---:|
| 学员扫码进入 | 3分钟内完成 |
| 100人同时进入 | 页面不崩溃 |
| 老师切换章节 | 正常网络下5秒内同步 |
| 弱网切换章节 | 15秒内最终一致 |
| 学员点击完成 | 不重复、不丢失 |
| 排行榜更新 | 10秒内完成 |
| 短暂断网恢复 | 30秒内恢复最新状态 |
| 老师查看状态 | 只需查看8-12行小组摘要 |

课堂内容采用按需加载：进入当前章节时加载当前内容，提前预加载下一章节，大文件单独下载。

## 12. 手机热点风险

100台手机连接一个手机热点时，现场无线网络可能比云端服务更容易成为瓶颈：

- 热点允许连接数量有限。
- 上行和下行带宽有限。
- 手机可能发热、降速或自动断开。
- 大量图片和文件同时加载会造成拥堵。

建议：

1. 课堂内容提前缓存。
2. 核心同步只传文字和状态。
3. 不把视频播放作为核心课堂链路。
4. 有条件时使用专用 5G 路由器或现场 Wi-Fi。
5. 上线前做 30人、60人、100人分阶段压力测试。

## 13. Vercel 使用边界

### 13.1 适合放在Vercel

- Next.js / React 前端。
- PWA 外框和静态资源。
- 课程目录和公开课程壳。
- 预览环境和自动部署。
- 轻量 API 路由。

### 13.2 不建议只放在Vercel

- 主数据库。
- 长时间 WebSocket 服务。
- 课堂事件队列。
- 大量敏感企业资料。
- 完全断网的本地课堂服务。

原型阶段可以使用 `vercel.app` 默认域名。进入企业试点后，建议使用自定义域名，并在真实培训现场测试网络可达性。企业敏感内容应放入受权限控制的私有存储，而不是公开静态目录。

## 14. MVP范围

### Must

- 老师创建课堂。
- 二维码和6位课堂码。
- 学员扫码进入。
- 创建和加入小组。
- 小组命名和临时组长。
- 导入课程目录和 HTML 内容。
- 三种跟随模式。
- 讲义、贴词和素材下载。
- 发布课堂任务。
- 学员点击完成。
- 老师查看小组完成矩阵。
- 学员提问，老师回答。
- 小组积分和排行榜。
- 弱网缓存和断线补同步。
- 课堂结束报告。

### Should

- 小组命名投票。
- 问题点赞和同类问题聚合。
- 多种积分规则。
- 课堂大屏。
- 奖章和成就。
- 任务质量评价。
- 课程模板复制。
- CSV 导出。

### Could

- AI问题聚类。
- AI回答草稿。
- AI课堂总结。
- 企业SSO。
- 工号和部门绑定。
- 本地课堂服务一键启动。

### Won't（第一版不做）

- 视频直播。
- 多老师同时授课。
- 学员开放式私聊。
- 复杂 LMS 功能。
- 自动替代老师讲课。
- 一开始支持任意 HTML 脚本。

## 15. 第一条垂直切片

```text
老师创建一堂课
→ 学员扫码进入
→ 学员加入小组
→ 老师切换到任务1
→ 学员查看HTML讲义
→ 学员复制贴词
→ 学员点击“已完成”
→ 老师看到8-12个小组的进度
→ 学员提交问题
→ 老师回答问题
→ 小组获得积分
→ 排行榜更新
```

## 16. 验收标准

- 学员从扫码到加入小组不超过 90 秒。
- 100 名学员同时进入时核心页面可用。
- 老师发布任务后，正常网络下 5 秒内同步。
- 弱网或短暂断网时，完成记录不会丢失。
- 网络恢复后不重复加分、不重复提交问题。
- 老师只看小组摘要就能判断谁已完成、谁卡住。
- 课程 HTML、贴词和素材可以从课程包加载。
- 三种跟随模式行为符合预期。
- 课堂结束后可以导出参与、完成、提问和积分结果。

## 17. 当前决策与待确认项

### 当前决策

- 产品形态：Web App / PWA。
- 课堂规模：最多100人，8-12组，单老师。
- 内容形式：版本化 HTML 课程包。
- 前端部署候选：Vercel。
- 数据和实时同步候选：Supabase。
- 弱网策略：IndexedDB缓存 + 事件队列 + 自动补同步。
- 锁定模式：强跟随、软跟随、自由浏览，默认软跟随。

### 待确认项

- 课程 HTML 是否包含敏感企业资料。
- 学员是否只使用昵称，还是需要工号或企业身份。
- 老师是否固定使用电脑和投影仪。
- 培训现场是否经常完全没有公网。
- 第一门试点课程的章节、任务和素材样例。
- 项目源码最终放置路径和技术栈确认。

## 18. 下一步

先冻结以下三个技术协议：

1. `manifest.json` 课程包格式。
2. `session_state` 课堂状态和锁定模式格式。
3. `sync_events` 学员操作同步事件格式。

完成后再制作一个最小原型，优先验证：

```text
老师切换章节
→ 学员按模式跟随
→ 学员点击完成
→ 老师看到小组进度
→ 断网后恢复同步
```

## 19. 原型阶段设计决策与迭代记录

> 本节用于沉淀每次原型讨论后的产品决策。当前阶段为原型阶段，优先验证课堂现场的主操作路径和界面结构，不代表最终视觉或生产实现。

### 19.1 2026-09-12 本轮确认

- 教师端默认主视图调整为“讲义放映台”，中间区域直接展示当前 PPT HTML，不再把小组大盘和讲义挤在同一屏。
- PPT HTML 由课程包或独立地址提供。教师端通过可嵌入容器展示，支持同域路径、局域网地址和 HTTPS 地址。
- 原型阶段保留一份内置 HTML 回退内容，用于无外部课程文件时演示章节切换和页码操作。
- 外部 HTML 页面可以使用自己的翻页逻辑。教师端不强制接管外部页面内部交互。
- 讲义支持全屏演示，当前章节和课堂状态仍由教师端控制。
- 左侧课程目录和右侧问题栏都支持折叠，给讲义留出更大的展示空间。
- 右侧学员问题支持点击放大到中间区域，教师可以直接写回答、公开回答并置顶、标记已解决，或返回讲义。
- 小组完成矩阵、积分和排行榜移动到独立的“课堂大盘”页面，避免干扰授课主线。
- 教师端界面可读文字最小字号统一为 12px，常规操作文字使用 14px 及以上。嵌入的 PPT HTML 字号由课程页面自行控制。

### 19.2 当前原型进度

| 模块 | 状态 | 说明 |
|---|---|---|
| 教师课堂控制台框架 | 已完成 | 左侧章节、中央讲义、右侧问题栏 |
| HTML 讲义展示 | 已完成 | 内置回退 HTML、外部 URL 嵌入、全屏演示 |
| 章节切换 | 已完成 | 切换后更新讲义内容和学生端预览 |
| 跟随模式 | 已完成 | 强跟随、软跟随、自由浏览 |
| 倒计时与发布任务 | 已完成 | 发布弹窗、暂停和继续倒计时 |
| 学员问题处理 | 已完成 | 问题放大、公开回答、解决状态 |
| 小组大盘 | 已完成 | 独立视图、指标、排行榜和小组详情 |
| 左右栏伸缩 | 已完成 | 课程栏和问题栏可折叠 |
| 真实课程包加载 | 待接入 | 需要接入 `manifest.json` 和安全资源地址 |
| Realtime / IndexedDB | 待接入 | 当前仅保留弱网演示入口，尚未接 Supabase |

### 19.3 下一步原型验证

1. 使用一份真实制作的 PPT HTML 课程文件验证嵌入、全屏和内部翻页。
2. 验证外部 HTML 与学生端同步时的页面标识协议，例如 `chapter_id`、`block_id` 和 `revision`。
3. 在教师授课视角下测试 12px、14px 和 PPT 内部字号，确认投影距离下的可读性。
4. 补齐讲义加载中、地址错误和跨域受限时的错误状态。
5. 冻结 `manifest.json`、`session_state`、`sync_events` 的字段后，再接入真实数据同步。

### 19.4 2026-09-12 课堂竞争与学生任务交互

- 课堂大盘增加“积分赛场”设计，使用前三名领奖台、即时积分和可投屏的高对比布局，增强课堂竞争与集体注意力。
- 课堂大盘明确拆分两个视角：
  - “当前任务执行”：展示本轮完成率、即时积分和需要支援的小组。
  - “全程任务完成”：展示从开场到当前章节的累计完成度和累计积分。
- 学生端预览改为真实任务卡结构，任务包含题目、说明、提示词和小组提交动作。
- 学生可以在预览中点击“我们组已完成”写入完成状态，也可以点击“提问”提交问题。完成和提问都保留本地事件队列的演示反馈。
- 左右侧栏伸缩按钮统一放在各自顶部工具线，保持同一高度、尺寸和对齐关系，折叠后保留窄栏入口。

### 19.5 当前原型进度补充

| 模块 | 状态 | 说明 |
|---|---|---|
| 积分赛场 / 领奖台 | 已完成 | 适合课堂大屏展示前三名和即时积分 |
| 当前任务执行视角 | 已完成 | 本轮进度、即时积分、需要支援 |
| 全程任务完成视角 | 已完成 | 累计完成度、累计积分、累计排行榜 |
| 学生任务卡 | 已完成 | 题目、提示、提示词、完成和提问 |
| 左右伸缩对齐 | 已完成 | 统一顶部工具线和折叠入口 |

### 19.6 2026-09-12 右侧问题栏折叠优化

- 右侧问题栏折叠后不再保留空的内容框或刷新工具，只保留一条窄的竖向入口。
- 竖向入口显示“课堂提问”和当前未处理数量，点击后恢复完整问题栏。
- 折叠入口与左侧课程栏的折叠按钮保持同一顶部工具线节奏，避免出现按钮漂移或两侧位置不对称。

### 19.7 2026-09-13 课堂大盘投屏模式

- 课堂大盘定位为直接投屏页面，不再按普通后台页面设计。
- 进入课堂大盘时自动收起左右侧栏，中央区域扩展到完整投屏画布；教师可通过“退出大屏”返回控制台。
- 投屏页面提升标题、指标、排行榜和小组状态的字号，保证会议室和教室后排可读。
- 增加现场感动效：前三名领奖台依次入场、LIVE 状态呼吸、积分赛场和倒计时作为视觉焦点。动效在减少动态偏好下自动降级。
- 大盘保留“当前任务执行”和“全程任务完成”两个视角，投屏时可以快速切换本轮赛况与累计排名。
- “大屏展示”按钮触发浏览器全屏能力；如果浏览器阻止全屏，仍保留扩展后的中央投屏布局。

### 19.8 当前原型进度补充

| 模块 | 状态 | 说明 |
|---|---|---|
| 课堂大盘投屏布局 | 已完成 | 自动隐藏两侧栏，中央扩展为投屏画布 |
| 领奖台入场动效 | 已完成 | 支持减少动态偏好降级 |
| 大屏倒计时 | 已完成 | 使用课堂统一倒计时 |
| 浏览器全屏入口 | 已完成 | 支持手动退出大屏 |

### 19.9 2026-09-13 课堂大盘头部收敛

- 课堂大盘顶部标题区缩小，避免投屏时头部占据过多垂直空间。
- “查看赛况”与当前任务 / 全程累计切换合并到同一条头部控制线，减少上下跳转。
- “学生跟随”只在讲义控制页显示，课堂大盘投屏页隐藏该控制，避免无关操作干扰观众。

### 19.10 2026-09-13 左侧投屏信息收敛

- 移除左侧“今日课堂”信息卡，避免重复展示已经在顶部状态区表达的课堂信息。
- 移除左侧“课程进度”标题行，章节列表直接从课程目录工具线开始，减少投屏时的层级噪音。
- 进入课堂大盘时左侧保留窄折叠栏和章节图标，中央保持投屏主画面，右侧问题栏保持收起。

### 19.11 2026-09-13 大盘进入方式修正

- “收起侧栏”和“切换课堂大盘”拆成两个独立动作，避免进入大盘时左侧导航直接消失。
- 教师先点击左侧课程栏的折叠按钮，再点击“课堂大盘”，中央切换为大盘且保留左侧窄栏。
- “大屏展示”仍然作为更强的投屏动作，只有点击后才隐藏右侧并请求浏览器全屏。

### 19.12 2026-09-13 课堂大盘自动收起左栏

- 点击左下角“课堂大盘”时，左侧课程栏自动收起为窄栏。
- 左侧不会消失，仍保留章节图标和入口，便于教师知道当前课程位置并重新展开。
- 中央区域同步切换为课堂大盘；该动作不等同于“大屏展示”，不会自动请求浏览器全屏。

### 19.13 2026-09-13 右栏纯图标折叠态

- 右侧问题栏收起后仅保留问号图标，不再显示竖排文字和数量标签，减少投屏时的视觉干扰。
- 图标保留可点击展开能力，并通过悬停提示告知当前待处理数量。

### 19.14 2026-09-13 右栏新问题提醒

- 右栏折叠时，如果存在待处理问题，在问号图标右上角显示语义化红点。
- 红点不显示具体数字，只负责提示有新问题；点击图标后在展开的问题栏查看数量和内容。

### 19.15 2026-09-13 课堂控制入口精简

- 将顶部的网络状态、授课状态、跟随模式、倒计时、大屏投放、导出和结束课堂等零散按钮统一收纳到“课堂状态”入口，减少重复状态和视觉噪音。
- 取消讲义页面单独的“学生跟随”控制条，跟随模式改在“课堂状态”菜单中切换。
- “发布本节任务”移动到当前章节标题右侧，与讲义来源和页码处于同一条工具线上。
- 发布任务弹窗明确展示当前章节、任务题目和提示词，避免发布错章节或只看到空泛的任务按钮。

### 19.16 2026-09-13 跟随与头部状态再精简

- 学生跟随模式只保留两种：`跟随` 和 `自由浏览`，移除强跟随 / 软跟随的区分，降低课堂现场的理解成本。
- 倒计时从状态菜单中提升到顶部可见位置，教师可以直接查看和暂停，不需要额外打开入口。
- 移除顶部的“投放课堂大屏”和“导出课堂快照”入口，减少非核心操作。
- 讲义标题右侧只保留“发布本节任务”，嵌入 HTML 保留在讲义设置位，不与发布动作竞争注意力。

### 19.17 2026-09-13 倒计时入口去重

- 倒计时已经固定显示在顶部，因此从“课堂状态”下拉菜单中移除重复的暂停 / 继续入口。
- 倒计时操作只保留顶部按钮，降低同一动作多处出现造成的认知负担。

### 19.18 2026-09-13 讲义标题栏再次精简

- 讲义标题右侧只保留“发布本节任务”，移除同步状态、页码、预览、刷新等重复或次要按钮。
- HTML 嵌入仍然保留，但移动到讲义底部的“讲义设置”入口，避免与发布任务抢占标题栏。

### 19.19 2026-09-13 学生端任务互动闭环

- 学生端预览明确拆分为“实操任务、互动题目、提示词”三个内容区，学生进入页面即可理解本节要做什么、如何回答、可以参考什么。
- 互动题目增加可点击答案选项，选中后即时反馈并记录选择，形成课堂互动的最小闭环。
- 实操任务保留“我们组已完成”按钮，点击后切换为已完成状态，再次点击可恢复进行中，便于演示状态流转。
- “提问”从即时提交改为展开提问输入框，学生填写问题后点击发送，教师端右侧问题列表可接收该事件。
- 学生端文字统一提升到 12px 及以上，手机预览区增加纵向空间，保证任务、选项和提示词在大字号下仍能完整展示。

### 19.20 2026-09-13 学生端多页面与课堂积分机制

- 学生端预览升级为四个底部固定标签：任务、互动、我的组、身份。切换标签不会离开手机容器，提问与“我完成了”固定在底部操作区，且完成状态只代表当前学生本人。
- “身份”页覆盖首次注册流程：学生选择所在小组、勾选是否为组长并保存；保存后仍可随时变更小组或身份。
- “我的组”页同时展示当前小组积分、本轮任务排名、当前任务积分排名和全任务累计排名，并展示组内完成进度。
- 互动积分规则采用轻量原型机制：答对互动题 +5 分，教师可在小组详情中按协作、高质量、突破三档加 3 / 5 / 10 分；积分即时反映到课堂大盘。
- 小组详情增加“本组最关心的 3 个问题”填写区。默认开启多人投票，组员可共同选择，组长确认保存；教师可在大盘点击任意小组查看并编辑。
- 学生端“我的组”页同步提供组长填写入口，组长可以选择并提交 3 个问题，组员看到投票提示后参与讨论。
- 新增统一“课堂配置”弹窗，包含课堂信息、教师信息、分组设置三个页面；分组设置可新增小组、编辑小组，并切换多人投票填写模式。

### 19.21 2026-09-13 学生端信息减法与导航命名

- 删除学生端任务、互动、小组和身份页面中的解释性长句，只保留任务内容、题目、选项、积分、状态和操作按钮。
- 底部标签统一为“任务 / 互动 / 小组 / 身份”，其中“小组”作为更短、更明确的入口名称。
- 底部操作区继续固定显示“提问”和“我完成了”，与页面内容区保持明确的上下分层。
- 增加模块间距和内容滚动边界，保证任务卡、题目卡、积分卡和身份卡在移动端有清晰的阅读节奏。
- 讲义练习页的完成提示同步改为“我完成了”，避免把个人完成状态误解为教师确认后的整组完成。

### 19.22 2026-09-13 学生端连接边界说明

- 当前原型中的“学生端预览”位于教师端右侧，仅用于演示移动端页面和交互状态，不是学生可访问的独立地址。
- 原型阶段教师端与学生预览共享浏览器内存状态，因此点击答题、提问、完成和组内填写可以即时演示，但尚未接入真实网络同步。
- 产品化连接方式仍按前文规划：教师创建课堂后生成课堂邀请码 / 二维码，学生通过独立学生端入口加入；正式实现时使用课堂会话、实时频道与离线事件队列完成同步。

### 19.23 2026-09-13 学生端独立页面入口

- 学生端已从教师端预览中抽出独立页面入口：`/student`。
- `/student` 使用独立的学生页面布局，包含任务、互动、小组、身份四个底部标签，以及固定的提问 / 我完成了操作区。
- 教师端右侧“学生端预览”保留为缩略演示，并增加“打开学生端”链接，便于在原型评审时快速切换真实页面视图。
- 当前独立页面仍使用本地原型状态；后续接入课堂邀请码、二维码和实时同步服务时，保留该 URL 结构作为学生加入入口。

### 19.24 2026-09-13 独立学生端主题修复

- 独立学生端补齐自己的浅色 / 深色主题变量，不再依赖教师端 `.app-shell` 的变量作用域。
- 全局页面最小宽度调整为由教师端保留，学生端可以在手机视口正常响应式布局，避免移动端出现空白或横向错位。

### 19.25 2026-09-13 学生端任务 / 锦囊 / 互动 / 我的重组

- 学生端“任务”页聚焦任务卡，并在页内切换“任务卡 / 锦囊”；锦囊按 01、02、03 步骤呈现提示词使用方法。
- “我完成了”移动到任务卡中央，完成状态只记录当前学生，不再把个人操作表达成小组完成。
- “互动”页只保留题目、选项和“提交”按钮，提交后显示简短结果反馈。
- 底部导航重组为四个入口：任务、互动、中央凸起的提问按钮、我的。提问按钮点击后弹出输入框，发送后进入个人提问清单。
- “身份”更名为“我的”，页面包含身份、所在小组、个人提问清单和个人进度情况，减少解释性文案。

### 19.26 2026-09-13 学生端底部导航与提问收起

- 底部导航最终固定为五项：任务、互动、提问、小组、我的；提问位于中间并保持凸起圆形样式。
- 提问输入框打开时增加透明点击遮罩，点击输入框之外的任意区域即可收起，不影响发送按钮和底部导航的可用性。

### 19.27 2026-09-13 学生端顶部空间与章节菜单

- 移除学生端顶部独立的信息带，避免“第几章 / 小组身份”与页面标题重复占用空间。
- 任务页将“任务”直接作为页内切换项，与“锦囊”并列，不再同时显示“任务 + 任务卡”两层重复标题。
- 顶部右侧改为三横线章节菜单，点击后选择不同章节；切换章节会回到任务卡视图并重置互动题状态。

### 19.28 2026-09-13 互动提交反馈与动效

- 互动题提交后显示短句反馈：答对时鼓励并展示 +5 分，其他答案显示已提交，避免使用长解释占据页面。
- 答对时增加一次性烟花闪光动效，作为奖励反馈；普通提交保留轻量状态反馈，不制造视觉噪音。
- 动效使用 Motion 实现，并遵循系统减少动画偏好，在 `prefers-reduced-motion` 场景下自动降级。
- 统一检查学生端字号、按钮高度、卡片圆角和模块间距，确保动效出现时仍保持内容层级清晰。

### 19.29 2026-09-13 学生端布局巡检与完成反馈

- 任务完成按钮增加按压缩放、悬停上浮和完成后的轻微弹性反馈，点击“我完成了”后沿用成功庆祝动效。
- 检查学生端顶部、内容区和底部导航的空间分配，确保任务卡标题、锦囊步骤和互动选项不会被固定底栏遮挡。
- 保持正文 14px 左右、辅助信息 12px、主要按钮 14px 以上的层级，避免动效和高对比色影响阅读。

### 19.30 2026-09-13 学生端吐司居中修复

- 修复学生端吐司提示偏离中心的问题：不再依赖会被动效覆盖的水平 `transform`，改用左右边界与自动外边距实现稳定居中。
- 保留 Motion 的垂直进入 / 退出动效，提示内容在不同视口宽度下保持视觉中心一致。

### 19.31 2026-09-13 任务完成燃烧转场

- 点击“我完成了”后，任务卡进入约 1.8 秒的燃烧收束动画：卡片逐渐变薄、上浮并淡出，底部加入暖色火焰光晕。
- 任务卡消失后只留下单句鼓励文案，并提供低干扰的“重新查看任务”入口，方便课堂演示重复体验。
- 动效沿用 Motion 进入反馈与 `prefers-reduced-motion` 降级策略，减少动画模式下不显示复杂燃烧过程。

### 19.32 2026-09-13 完成转场抖动修复

- 移除完成按钮在燃烧阶段的悬停上浮和弹性缩放叠加，避免鼠标停留触发 hover 反复竞争。
- 燃烧关键帧改为单一的 `translate3d + scale` 位移，并增加合成层提示，减少 transform、滤镜和裁剪同时变化造成的抖动。

### 19.33 2026-09-13 完成反馈信息收敛

- 完成任务时取消顶部鼓励层和底部吐司的双重文案，避免同时遮挡任务标题和底部导航。
- 成功路径只保留烟花视觉反馈，燃烧结束后在任务卡原位置显示单句鼓励语；“重新查看任务”作为低干扰恢复入口。
- 燃烧阶段按钮锁定并显示“完成中”，避免用户重复点击造成状态抖动或重复提交。

### 19.34 2026-09-13 互动页答案布局修复

- 修复答案文案误套圆形标记样式导致的逐字换行问题，答案字母与答案文本拆分为独立布局元素。
- 互动题标题字号收敛到 36px 上限，提交按钮和反馈信息保持同一中心轴，减少视觉偏移。

### 19.35 2026-09-13 提交后反馈去重

- 互动题提交后取消顶部鼓励浮层和底部“答案已提交”吐司的叠加，结果统一留在题目卡内。
- 答对时保留烟花动效与卡内积分反馈；答错时只显示“已提交，继续尝试下一题”，减少遮挡和信息噪音。
- 提交按钮进入“已提交”状态，只有重新选择答案后才恢复可提交，避免重复提交。

### 19.36 2026-09-13 学生端移动 UI 规范化

- 移动端组件库方案确定为继续使用项目现有的 Radix Themes + Phosphor Icons，不在同一页面混用 Ant Design Mobile，避免按钮、圆角、颜色和交互状态出现两套语言。
- 在学生端根节点新增移动设计 token：4 / 8 / 12 / 16 / 20 / 24 / 32px 间距级别、8 / 12 / 16px 圆角级别、12 / 14 / 16 / 20px 字号层级，以及 44px 最小触控尺寸。
- 任务 / 锦囊切换、互动答案、提交按钮、组长保存、提问输入与底部导航统一使用 token，保证不同页面的节奏和点击热区一致。
- 所有学生端按钮和输入框增加统一 focus-visible 键盘态；按钮补充 pressed、hover、disabled 状态，提问输入和底部操作区兼容移动端安全区。
- Ant Design Mobile 作为后续产品化迁移备选，仅在需要完整移动端组件生态时评估，不纳入当前原型依赖。

### 19.37 2026-09-13 学生端“课堂剪辑台”视觉重设计

- 学生端采用“课堂剪辑台”作为移动端视觉隐喻：章节和任务是可定位的工作轨道，当前任务用胶带色标记，完成反馈像剪辑完成后的确认动作。
- 保留“共场”青绿色品牌色作为主交互色，仅使用低面积橙色作为当前片段 / 任务定位标记，避免多色抢夺注意力。
- 顶部章节菜单与底部固定导航形成上下两条稳定轨道，内容区保持单列、可滚动、无遮挡；任务卡、题目卡和小组信息沿同一内容基线排列。
- 浅色模式使用冷灰纸面与白色工作面，深色模式使用碳黑工作台；两种主题共享同一间距、字号、圆角和触控规范。
- 动效只用于用户触发的状态变化：切换章节、选择答案、完成任务和提交提问；减少动态效果偏好下自动降级。
- 本轮为学生端视觉世界替换，产品事实、五项导航、任务 / 互动 / 提问 / 小组 / 我的功能和原型状态保持不变。

### 19.38 2026-09-13 学生端小组与我的信息结构精修

- 小组页将“本轮完成情况 / 本轮任务组内进度 / 全任务排名”合并为同一行指标区，减少重复卡片和纵向滚动。
- “本组最关心的 3 个问题”改为 1、2、3 编号输出列表，默认直接展示三条问题；编辑时使用整行选中态，不再显示打勾图标。
- “我的”页调整为“我的名称 → 我的组别 → 我的身份 → 我的提问 → 我的进度”的顺序，先确认个人身份，再查看个人课堂行为。
- 首次进入“我的”页增加名称输入；保存后以三行资料摘要展示名称、组别和身份，并提供“编辑资料”入口。

### 19.39 2026-09-13 小组排名信息去重

- 小组页顶部只保留当前积分与小组标识，移除重复的“当前任务排名”。
- 排名信息统一收纳到同一行的三个指标中，分别对应本轮完成情况、组内进度和全任务排名。

### 19.40 2026-09-13 组长讨论后编辑提交

- 小组问题从“选项勾选”调整为“组长编辑模式”：默认状态直接展示 3 条问题，组长点击“编辑本组问题”后进入三行输入表单。
- 输入表单明确提示“组长与组员讨论后，由组长填写 3 个问题”，提交前校验三项均已填写；取消可放弃本次编辑。
- 非组长保持只读状态，仅查看已提交问题和“由组长与组员讨论后填写”的状态说明。
- 底部中央提问按钮固定为 64×64px 圆形，并取消内部默认内边距，保证视觉轮廓完整、点击区域稳定。

### 19.41 2026-09-13 组长身份文案收敛

- 学生端首次资料填写中的组长选项统一改为“我是组长”，使用第一人称直接表达选择，避免“我的身份是组长”的冗余表述。

### 19.42 2026-09-13 小组名称编辑

- 组长进入“编辑小组资料”后，可同时修改小组名称和本组最关心的 3 个问题。
- 名称与问题作为一次小组资料提交统一校验，任一字段为空时不提交；组员保持只读查看。

### 19.43 2026-09-13 学生端任务标题与边线统一

- 学生端顶部和底部固定操作栏的边线统一使用品牌青绿色，形成清晰的上下操作边界。
- 任务页改为与互动、小组、我的一致的页面标题结构，左侧显示“任务 / 本节任务”，右侧保留带 Sparkle 图标的“锦囊”入口。
- “锦囊”继续按 01、02、03 步骤展示提示词方法，并在底部显示本节可复制的完整提示词。

### 19.44 2026-09-13 页面标题层级精修

- 学生端页面标题字号收敛为桌面最大 30px、手机 24px，避免标题压过任务内容和操作入口。
- 页面标题字重调整为 650，颜色使用正文色与辅助色的轻混合，保持可读性同时降低视觉压迫感。
- 任务卡内的实际任务标题保持原有大字号，继续作为当前页面的主要阅读焦点。

### 19.45 2026-09-13 小组名称编辑入口前置

- 组长在“小组”标题右侧可直接点击圆形编辑图标，进入小组名称与 3 个问题的编辑模式。
- 组员不显示编辑入口，继续保持只读查看，避免误操作。

### 19.46 2026-09-13 小组名称原地编辑

- 小组标题统一显示为“小组：{名称} 编辑”，组长可直接点击标题旁的文字按钮进入编辑。
- 编辑完成后使用“保存”提交，不再使用独立图标或额外入口表达相同动作。

### 19.47 2026-09-13 小组标题原地输入

- 点击标题旁“编辑”后，标题中的小组名称直接替换为输入框，保持原位置和上下文不变。
- 输入框右侧仅保留“完成”按钮，点击后立即保存名称并回到标题展示态。
- 小组问题编辑仍通过“编辑小组资料”入口进行，避免名称修改和问题填写互相干扰。

### 19.48 2026-09-13 游戏化任务卡信息层级

- 任务卡补充“任务卡 / 进行中”状态徽章，强化当前挑战的识别。
- 任务正文下方直接展示本节场景说明，帮助学生先理解为什么做、在什么背景下做。
- 新增三段式任务流程：读懂场景 → 完成实操 → 提交结果；流程作为辅助引导，不替代实际任务内容。
- 任务标题字号提升至移动端约 28px、桌面端最高 42px，场景说明保持 16px，形成游戏任务卡的主次层级。

### 19.49 2026-09-13 学生端全局字号规范

- 学生端统一采用四级字号：任务 / 互动主标题 28–42px，页面标题 22–30px，正文 16px，按钮与区块标签 13–14px，辅助说明 12px。
- 所有字号同时配套固定行高和字重，避免仅调整大小后造成阅读密度或垂直节奏不一致。
- 页面级标题保持轻量，任务卡标题保持突出，正文和操作文字优先保证手机端可读性。

### 19.50 2026-09-13 任务卡内容收敛

- 任务卡正文只保留任务标题和一段场景说明，用一句话说明这是什么任务以及当前背景。
- 移除任务卡内的状态徽章、三步流程、时间和协作标签，避免任务内容被辅助信息切碎。
- “锦囊”页面继续独立承载分步骤提示词，不与任务卡正文混排。

### 19.51 2026-09-13 学生端字号规范覆盖修正

- 学生端统一采用 `12 / 14 / 16 / 20px` 的基础字号阶梯：辅助信息 12px、按钮与标签 14px、正文 16px、结果与鼓励文案 20px。
- 页面标题使用 `clamp(22px, 3vw, 30px)`，手机端固定为 24px；任务卡标题使用 `clamp(28px, 5vw, 42px)`，手机端收敛到 28–34px。
- 将章节状态、提问提示、排名辅助信息等局部 11px/13px 文案提升到 12px/14px，避免在手机端出现难读的小字。
- 规范作为样式文件末尾的最终覆盖层，确保组件样式和移动端媒体查询不会重新引入不一致字号；保留 44px 最小触控高度和对应行高。

### 19.52 2026-09-13 教师端学生预览改为独立页面

- 教师端“学生端预览”不再在右侧面板内重复渲染手机模拟器，改为单一的“打开学生端”入口。
- 点击入口使用新窗口打开 `/student`，直接查看真实学生端页面，避免预览状态与实际页面产生差异。
- 预览面板保留学生端图标、独立页面提示和明确的箭头动效，保证入口易发现且不增加无效信息。

### 19.53 2026-09-13 课堂配置弹窗层级与班级分组设置

- 修复教师信息弹窗背景变量作用域错误：弹窗内容改为不透明工作面，遮罩下不再透出课件标题和表单文字。
- “课堂信息”配置明确拆分为班级名称、课程主题、课堂说明、默认任务时长和课堂邀请码，保存按钮文案同步为“保存班级信息”。
- 保留“教师信息”和“分组设置”作为同级配置页，教师可在同一入口完成个人资料、班级信息与分组规则设置。

### 19.54 2026-09-13 课程生命周期与 Excel 导出入口

- 在教师端课堂状态菜单增加“课程管理”分组，提供“开设新课”“导出当前课程 Excel”“结束课程”三个生命周期动作。
- “开设新课”重置章节、计时器、发布状态、小组和提问数据，并自动打开班级信息配置作为新课根节点。
- “结束课程”增加二次确认，结束后停止计时并保留数据导出能力；顶部状态同步显示“课程已结束”。
- 导出文件包含小组进度、状态、积分、备注和课堂提问，使用 Excel 可直接打开的 `.xls` 文件格式下载。

### 19.55 2026-09-13 课程切换与课程包导入

- 课程管理菜单增加课程切换列表，显示课程名称、班级名称和章节数量，当前课程用勾选和状态点标识。
- 课程目录数据升级为课程包结构：每门课程包含章节目录、每章场景说明 / 实操任务 / 提示词，以及独立互动题目和选项。
- 增加“导入课程信息（JSON）”入口，导入后自动生成草稿课程并切换到该课程，教师可先完善班级信息再发布。
- 课程导出同步包含课程目录和互动题目，保证导入 / 编辑 / 导出形成完整的原型闭环。

### 19.56 2026-09-14 新增课程与编辑课程配置分流

- “开设新课”进入独立的“新增课程配置”模式：先填写班级名称、课程主题和课堂说明，点击“创建课程”后才生成新的草稿课程。
- “编辑课程配置”只更新当前课程，不会创建新课程；从课堂配置菜单或教师信息入口进入，保存按钮明确为“保存课程配置”。
- 新增模式下教师信息 / 分组设置页不允许提前提交，提示先完成课程基础信息，避免把新建和编辑动作混在一起。
- 课程切换列表的双行信息增加固定行高和间距，课程名、班级名、章节数量与当前状态不再发生视觉重叠。

### 19.57 2026-09-14 原型功能界面覆盖审计

已覆盖的核心页面 / 界面：

- 教师讲义工作区：HTML / PPT 嵌入、章节目录、当前章节任务发布。
- 课堂大盘：当前任务 / 全程累计、分组进度、排行榜、点击小组查看详情、教师加分和大屏模式。
- 教师提问处理：问题列表、红点提醒、问题聚焦、公开回答、标记已解决。
- 学生端独立页面 `/student`：任务、互动、提问、小组、我的；任务完成动效、互动提交、组长编辑小组名称和 3 个问题。
- 课程配置：新增课程、编辑课程、切换课程、JSON 课程包导入、结束课程、Excel 数据导出。

尚未覆盖或仅为局部演示的界面，按优先级排序：

P0 — 下一轮原型必须补齐：

- 学生入场页：扫码 / 6 位课堂码、昵称、创建或加入小组、首次身份确认、二维码展示页。
- 课程内容编辑页：课程目录树、章节排序、每章 HTML / 任务 / 互动 / 提示词编辑，以及课程包 manifest / assets 导入向导。
- 分组管理页：新增 / 删除 / 编辑小组、成员列表、组长转移、名称候选和投票状态；当前按钮仍是演示 toast。
- 互动题编辑页：题型、选项、正确答案、分值和发布状态；当前每门课只有一个静态互动题。
- 课程导入预览页：当前 JSON 导入直接切换课程，缺少 manifest 校验、章节预览和导入失败定位。

P1 — 进入真实课堂前补齐：

- 课堂连接与同步状态页：强跟随 / 软跟随 / 自由浏览、当前章节指针、学生锁定和断线重连提示。
- 教师回答页与学生结果页：老师输入回答、公开 / 仅小组可见、学生查看回答和问题状态流转。
- 积分明细与奖励页：加分 / 扣分事件、积分理由、荣誉标签、最佳协作组等激励反馈。
- 课堂结束报告页：参与率、完成率、提问处理率、积分排行和导出前预览；当前只有直接下载 Excel。
- 教师资料持久化页：教师姓名、角色、寄语编辑后即时更新教师工作区头像和资料摘要（原型内存状态，后端持久化待产品化）。

P2 — 产品化阶段再补齐：

- 课程版本 / 草稿 / 已发布 / 已归档管理页，以及课程复制和版本回滚。
- 素材管理与下载页：图片、文件、模板和权限控制；当前学生端只有提示词复制。
- 老师 / 学生账号与权限页、实时事件审计页、弱网缓存和同步队列状态页。

当前原型只有 `/` 教师端和 `/student` 学生端两个入口；以上缺口不是视觉细节，而是需要独立页面或完整状态流转的功能边界。

### 19.58 2026-09-14 P0 页面补齐

- 学生端增加独立入场页：课堂码、昵称、加入小组、组长身份和进入课堂动作；进入后才展示任务 / 互动 / 提问 / 小组 / 我的主界面。
- 教师端课程配置增加“课程内容”页：章节列表、章节新增 / 删除、章节标题、场景说明、实操任务、提示词和互动题编辑，保存后更新当前课程。
- 教师端“分组设置”从演示按钮升级为可新增小组、原地编辑小组名称并保存的管理列表。
- 课程包继续支持 JSON 导入，导入后的课程可以直接进入课程内容编辑页补全目录和互动；P0 页面以原型状态流转为目标，尚未接入后端持久化。

### 19.59 2026-09-14 P1 首批闭环

- 新增“课堂连接与同步”弹窗：教师可查看网络、在线人数和当前章节，切换跟随 / 自由浏览，并模拟锁定学生章节与立即同步。
- 提问处理从“直接移除”升级为回答闭环：教师在问题聚焦页填写回答，选择公开给全班或仅该小组，并可发送后继续处理或发送并标记解决。
- 学生提问通过本地事件通道写入教师端问题列表；教师回答会写入本地事件通道，学生端出现“老师已回复”提示，原型阶段可跨标签页演示状态流转。
- 小组详情新增积分明细，记录加分理由、分值和时间；新增课堂结束报告弹窗，展示参与率、完成率、提问处理率、积分排行、课堂荣誉和 Excel 导出入口。
- JSON 课程导入改为“解析 → 预览 → 确认导入”，预览展示课程名称、班级、章节数量、互动选项和章节任务摘要，格式错误仍在选择文件后即时提示。
- 积分规则纳入课程配置：教师可设置任务完成、互动答对、有效提问和教师额外奖励的分值，保存后写入原型本地规则并供学生端反馈使用。

### 19.60 2026-09-14 原型界面打磨审查

- 修复课堂配置、积分规则、导入预览等弹窗的层级与不透明背景，避免课件大标题穿透造成文字重叠。
- 弹窗统一增加独立阴影、遮罩和内容隔离，保留底层页面位置感但不让底层文本干扰阅读。
- 当前仍建议在进入真实课堂前继续做一次键盘焦点、窄屏滚动、课程切换后学生内容一致性和空状态文案验收。

### 19.61 2026-09-14 独立课程管理页

- 左侧底部新增“课程管理”入口，点击后进入独立 `/manage` 页面，不打断课堂讲义和课堂大盘。
- 管理页按“课程 → 章节 → 提示词”三级结构组织内容；每个章节支持多个提示词，可新增、编辑、删除并保存。
- 课程目录和内容编辑从课堂操作区分离，便于教师课前集中维护；课堂页继续只承担当前章节发布与实时互动。
- 教师端左下角入口收口为“教师信息”弹窗；原课程、分组、积分等设置弹窗不再从该入口展示，统一迁移到右上角菜单中的独立管理页。

### 19.62 2026-09-14 管理页布局修复

- 管理页顶部分类标签从覆盖式固定层改为正常文档流中的吸顶导航，主内容为标签栏预留 57px 高度，避免标题、课程目录和编辑区发生垂直错位。

### 19.63 2026-09-14 双层头部对齐

- 管理页采用双层固定头部：第一层为返回与页面身份，第二层为课程内容 / 课程信息 / 分组设置 / 积分规则 Tab；内容区从第二层之后开始，不再出现标签漂浮或压住标题。
- 管理页新增“互动题目”二级入口，按章节切换并编辑独立题目、选项和正确答案；未覆盖的章节继续使用课程默认题。

### 原型进度补充（2026-09-14）

- 课程管理页的课程目录与内容编辑区完成对齐，目录固定在左、章节内容固定在右。
- 章节内容统一通过“实操任务 / 提示词 / 互动题目”三个标签管理，支持每章多个提示词和独立互动题，移除重复入口。
